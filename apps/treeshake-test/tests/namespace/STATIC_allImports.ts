import { tgpu } from 'typegpu/$built$';
import * as d from 'typegpu/data/$built$';
import * as std from 'typegpu/std/$built$';
import * as common from 'typegpu/common/$built$';

console.log(tgpu, d, std, common);
