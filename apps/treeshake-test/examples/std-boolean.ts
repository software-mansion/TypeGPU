// Standard library - boolean operations
import { all, any, select, eq, ne, lt, gt } from 'typegpu/std';

console.log('Boolean functions:', {
  all: typeof all,
  any: typeof any,
  select: typeof select,
  eq: typeof eq,
  ne: typeof ne,
  lt: typeof lt,
  gt: typeof gt,
});