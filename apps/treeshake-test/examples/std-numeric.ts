// Standard library - numeric functions
import { sin, cos, max, min, sqrt, abs } from 'typegpu/std';

console.log('Math functions:', {
  sin: typeof sin,
  cos: typeof cos,
  max: typeof max,
  min: typeof min,
  sqrt: typeof sqrt,
  abs: typeof abs,
});