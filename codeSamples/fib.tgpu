---
import { rand01 } from './random.tgpu';
---

@export fn fib(n: u32): u32 {
  var a: u32 = 0;
  var b: u32 = 0;

  for (var i = 0; i < n; i += 1) {
    var tmp = a;
    a = b;
    b = tmp + b;
  }

  return a;
}
