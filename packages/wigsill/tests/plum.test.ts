import { describe, it } from 'vitest';
// import { createRuntime, wgsl } from 'wigsill';

describe('wgsl.plum', () => {
  it('should hold initial value', async () => {
    //   const runtime = await createRuntime();
    //   const foo = wgsl.plum(123);
    //   expect(foo.read(runtime)).toEqual(123);
  });
});
